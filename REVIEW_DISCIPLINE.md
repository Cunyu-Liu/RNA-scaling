# 复查纪律 (用户指令 2026-09-15)

1. 每个科学数字/结论产出前, 必须从原始数据 (JSONL/日志/manifest) 独立
   重算至少 3 遍, 不得从中间汇总文件抄录。
2. 选行/选点规则必须显式 (如: 最终 ckpt + n_train>=4000), 不得默认
   "取最新" 或 "取最大" 而不声明依据字段。
3. 同一 run 多次 probe 的行 (不同 ckpt_nt / n_train / 协议版本) 必须
   先按规则去重, 再统计。
4. 汇总脚本改动后必须跑 PASS-6 式全量对照 (手算参考 vs 脚本输出)。
5. smoke test / proxy 指标 / 训练集数字永不写成最终结论。
